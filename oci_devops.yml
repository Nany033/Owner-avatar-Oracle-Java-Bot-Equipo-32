version: 0.1
component: build
timeoutInSeconds: 3600
shell: bash

env:
  variables:
    JAVA_HOME: /usr/lib64/graalvm/graalvm22-ee-java17
    TODO_PDB_NAME: ${TODO_PDB_NAME}
    REGION: ${REGION}
    UI_USERNAME: ${UI_USERNAME}
    DOCKER_USERNAME: ${DOCKER_USERNAME}
    DOCKER_PASSWORD: ${DOCKER_PASSWORD}
    DOCKER_REGISTRY: ${REGION}


  exportedVariables:
    - DOCKER_USERNAME
    - DOCKER_PASSWORD
    - TODO_PDB_NAME
    - REGION
    - UI_USERNAME
    - DOCKER_REGISTRY

steps:
  - type: Command
    name: "Install GraalVM Enterprise 22.x Native Image for Java17"
    command: yum -y install graalvm22-ee-17-native-image

  - type: Command
    name: "Set PATH Variable"
    command: export PATH=$JAVA_HOME/bin:$PATH

  - type: Command
    name: "Docker Login"
    command: |
      echo "x6Lx61Y(Md_uTTvO(kVa" | docker login -u "axixsctxuqtl/a01637441@tec.mx" --password-stdin mx-queretaro-1.ocir.io

  - type: Command
    name: "Build and Push Docker Image"
    command: |
      cd MtdrSpring/backend
      export DOCKER_REGISTRY=mx-queretaro-1.ocir.io
      ./build.sh

  - type: Command
    name: "Deploy to Kubernetes"
    command: |
      cd MtdrSpring/backend
      ./deploy.sh



